import { WebSocketConnectionProvider } from "@theia/core/lib/browser";
import { Workspace, Languages, Window, ILanguageClient, LanguageClientOptions, LanguageContribution } from '../common';
export declare class LanguageClientFactory {
    protected readonly workspace: Workspace;
    protected readonly languages: Languages;
    protected readonly window: Window;
    protected readonly connectionProvider: WebSocketConnectionProvider;
    constructor(workspace: Workspace, languages: Languages, window: Window, connectionProvider: WebSocketConnectionProvider);
    get(contribution: LanguageContribution, clientOptions: LanguageClientOptions): ILanguageClient;
}
