import { DefaultUriLabelProviderContribution } from "@theia/core/lib/browser/label-provider";
import URI from "@theia/core/lib/common/uri";
import { WorkspaceService } from "./workspace-service";
import { FileSystem, FileStat } from "@theia/filesystem/lib/common";
export declare class WorkspaceUriLabelProviderContribution extends DefaultUriLabelProviderContribution {
    protected fileSystem: FileSystem;
    wsRoot: string;
    constructor(wsService: WorkspaceService, fileSystem: FileSystem);
    canHandle(element: object): number;
    private getUri(element);
    private getStat(element);
    getIcon(element: URI | FileStat): Promise<string>;
    getName(element: URI | FileStat): string;
    /**
     * trims the workspace root from a file uri, if it is a child.
     */
    getLongName(element: URI | FileStat): string;
}
