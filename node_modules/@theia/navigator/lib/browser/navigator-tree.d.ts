import { FileSystem } from "@theia/filesystem/lib/common";
import { FileTree } from "@theia/filesystem/lib/browser";
import { ITreeNode, ICompositeTreeNode } from '@theia/core/lib/browser/tree/tree';
import { FileNavigatorFilter } from './navigator-filter';
export declare class FileNavigatorTree extends FileTree {
    protected readonly fileSystem: FileSystem;
    protected readonly filter: FileNavigatorFilter;
    constructor(fileSystem: FileSystem, filter: FileNavigatorFilter);
    resolveChildren(parent: ICompositeTreeNode): Promise<ITreeNode[]>;
}
