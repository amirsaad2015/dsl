import { VirtualWidget, Message } from "@theia/core/lib/browser";
import { VirtualElement } from "@phosphor/virtualdom";
import { OutputChannelManager, OutputChannelImpl } from "../common/output-channel";
import "../../src/browser/style/output.css";
export declare const OUTPUT_WIDGET_KIND = "outputView";
export declare class OutputWidget extends VirtualWidget {
    protected readonly outputChannelManager: OutputChannelManager;
    private selectedChannel;
    constructor(outputChannelManager: OutputChannelManager);
    protected onActivateRequest(msg: Message): void;
    protected registerListener(outputChannel: OutputChannelImpl): void;
    render(): VirtualElement[];
    private readonly OUTPUT_CONTENTS_ID;
    protected renderChannelContents(): VirtualElement;
    protected toHtmlText(text: string): VirtualElement[];
    private readonly NONE;
    protected renderChannelSelector(): VirtualElement;
    protected onUpdateRequest(msg: Message): void;
}
