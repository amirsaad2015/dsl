import { EditorManager, EditorDecorationsService, EditorDecoration, EditorDecorationOptions } from '@theia/editor/lib/browser';
import { DirtyDiffUpdate } from './dirty-diff-manager';
import { LineRange } from './diff-computer';
export declare enum DirtyDiffDecorationType {
    AddedLine = "dirty-diff-added-line",
    RemovedLine = "dirty-diff-removed-line",
    ModifiedLine = "dirty-diff-modified-line",
}
export declare class DirtyDiffDecorator {
    protected readonly editorManager: EditorManager;
    protected readonly editorDecorationsService: EditorDecorationsService;
    constructor(editorManager: EditorManager, editorDecorationsService: EditorDecorationsService);
    applyDecorations(update: DirtyDiffUpdate): void;
    protected appliedDecorations: Map<string, string[]>;
    protected setDecorations(uri: string, newDecorations: EditorDecoration[]): Promise<void>;
    protected toDeltaDecoration(from: LineRange | number, options: EditorDecorationOptions): EditorDecoration;
}
