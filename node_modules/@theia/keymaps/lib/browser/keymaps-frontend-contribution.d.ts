import { CommandContribution, Command, CommandRegistry, MenuContribution, MenuModelRegistry } from '@theia/core/lib/common';
import { OpenerService } from '@theia/core/lib/browser';
import { FrontendApplication } from '@theia/core/lib/browser';
import { WidgetManager } from '@theia/core/lib/browser/widget-manager';
export declare namespace KeymapsCommands {
    const OPEN_KEYMAPS: Command;
}
export declare class KeymapsFrontendContribution implements CommandContribution, MenuContribution {
    protected readonly app: FrontendApplication;
    protected readonly widgetManager: WidgetManager;
    protected readonly openerService: OpenerService;
    constructor(app: FrontendApplication, widgetManager: WidgetManager, openerService: OpenerService);
    registerCommands(commands: CommandRegistry): void;
    registerMenus(menus: MenuModelRegistry): void;
    protected openKeymapsFile(): void;
}
