import { Workspace } from "@theia/languages/lib/browser";
import { MergeConflictsParser } from "./merge-conflicts-parser";
import { MergeConflict } from "./merge-conflict";
import { Event } from '@theia/core/lib/common/event';
export declare class MergeConflictsService {
    protected readonly workspace: Workspace;
    protected readonly mergeConflictParser: MergeConflictsParser;
    private onMergeConflictUpdateEmitter;
    constructor(workspace: Workspace, mergeConflictParser: MergeConflictsParser);
    get(uri: string): MergeConflict[];
    protected getMergeConflicts(uri: string): MergeConflict[];
    readonly onMergeConflictUpdate: Event<MergeConflictUpdateParams>;
}
export interface MergeConflictUpdateParams {
    uri: string;
    mergeConflicts: MergeConflict[];
}
