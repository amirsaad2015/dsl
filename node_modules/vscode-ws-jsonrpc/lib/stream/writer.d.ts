import { Message } from "vscode-jsonrpc/lib/messages";
import { MessageWriter, AbstractMessageWriter } from "vscode-jsonrpc/lib/messageWriter";
export declare abstract class AbstractStreamMessageWriter extends AbstractMessageWriter implements MessageWriter {
    write(msg: Message): void;
    protected writeMessage(msg: Message): string;
    protected abstract send(content: string): void;
}
