import { AbstractMessageReader, DataCallback } from "vscode-jsonrpc/lib/messageReader";
export declare abstract class AbstractStreamMessageReader extends AbstractMessageReader {
    abstract listen(callback: DataCallback): void;
    protected readMessage(message: any, callback: DataCallback): void;
}
